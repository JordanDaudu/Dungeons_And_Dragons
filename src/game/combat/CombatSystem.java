package game.combat;

public class CombatSystem {

    public void resolveCombat(Combatant attacker, Combatant defender) {
        if(attacker instanceof MeleeFighter) {
            if(((MeleeFighter) attacker).isInMeleeRange(attacker.))
        }
    }
}
